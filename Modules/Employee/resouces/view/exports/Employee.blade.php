{{-- <table>
    <thead>
        <tr>
            <th>No</th>
            <!-- Add more headers as needed -->
        </tr>
    </thead>
    @foreach ($data as $item)
        <tr>
            <td>{{ $loop->iteration }}</td>
            <!-- Add more columns as needed -->
        </tr>
    @endforeach
</table> --}}
